package kuke.board.common.event;

/**
 * 이벤트 페이로드(payload)를 나타내는 마커(marker) 인터페이스입니다.
 * 이 인터페이스를 구현하는 클래스는 이벤트의 일부로 사용될 수 있는 데이터 객체임을 명시합니다.
 * 마커 인터페이스는 특정 메서드를 강제하지 않고, 타입 구분을 위해 사용됩니다.
 */
public interface EventPayload {
}